# UK environment template. Copy to .env.uk and fill in real values, then run any script with
# ENV_FILE=.env.uk set (see src/config/env.ts) to target this environment instead of the default
# US one in .env.example/.env. .env.uk is gitignored and must never be committed.
#
# PowerShell: $env:ENV_FILE='.env.uk'; npm run test:chrome
# bash:       ENV_FILE=.env.uk npm run test:chrome
#
# UNVERIFIED — filled in from the one URL given (https://uk.test.givergy.com/manage/#events) plus
# the same domain-swap pattern the US values follow; confirm every value below against the real UK
# environment before relying on a run against it. In particular: CMS_BASE_URL's path is `/manage/`
# here, not `/cms-next/` like the US environment — this was confirmed to be the same underlying
# app on a different domain, not a different product, but the existing Page Object Model (every
# locator in src/pages/cms/) was built and verified only against `/cms-next/`'s DOM. If UK's
# `/manage/` app renders differently, page objects will need real fixes, not just this config swap.

ENV_NAME=uk-integration

# CMS (cms-next) under test. Must end with a trailing slash — Playwright resolves
# leading-slash relative paths (e.g. page.goto('/login/')) against the origin root,
# not this path, so page objects use relative paths without a leading slash instead.
CMS_BASE_URL=https://uk.test.givergy.com/manage/
CMS_USERNAME=
CMS_PASSWORD=

# A pre-existing test campaign/event in the UK environment, reused across runs. Must be created by
# hand first — there is no UK equivalent of the US TEST_EVENT_ID yet.
TEST_EVENT_ID=

# Lite UI (public-facing) test campaign — the live website slug for TEST_EVENT_ID above.
LITE_UI_BASE_URL=
LITE_UI_SKIP_HOLDING=

# ---------------------------------------------------------------------------
# Fundraising / outcome suite (tests/e2e, tests/api). Needs a DEDICATED, clean UK event and a full
# set of fixtures (ticket/lots/raffle/donation-ready guest) created and verified live the same way
# the US ones were — see README's "Current coverage" fixture notes for what each one needs to look
# like. None of the US E2E_* ids below are reusable across environments; leave blank until UK
# fixtures exist.
E2E_EVENT_ID=
E2E_LITE_UI_BASE_URL=
E2E_API_GUEST_ID=
E2E_TICKET_ID=
E2E_LOT_ID=
E2E_BUYNOW_LOT_ID=
E2E_SEALED_LOT_ID=
E2E_RAFFLE_ID=

# Back-end APIs (same host as the UIs). No trailing slash. UNVERIFIED — assumed from the same
# path pattern as the US environment (EMS_API_BASE_URL/LITE_API_BASE_URL there are /ems, /lite
# off the same host as CMS_BASE_URL/LITE_UI_BASE_URL); confirm against the real UK host.
EMS_API_BASE_URL=https://uk.test.givergy.com/ems
LITE_API_BASE_URL=https://uk.test.givergy.com/lite
